# 精力管理系统 | Energy Management System

一个基于 PyQt5 的精力分配和任务管理应用。

## 快速启动

```bash
# 进入项目目录
cd /Users/likeju/Desktop/project/energy_distribution

# 激活虚拟环境
source venv/bin/activate

# 运行应用
python main_pyqt5.py
```

## 功能特性

### 📅 精力分配统计
- 按日期记录时间分配
- 支持多种时间格式输入（1.5, 1h30m, 90m）
- 实时饼图可视化
- 日期导航和快速操作
- 四列布局，节省空间

### 📈 周/月/年统计
- 查看不同时间周期的统计数据
- 周、月、年三种统计模式
- 自动数据聚合
- 时间周期导航

### 📊 精力管理
- 四象限任务管理（重要性 × 紧急性）
- 任务优先级排序
- 任务完成状态管理
- 象限间任务移动

## 数据存储

所有数据保存在 `data/` 目录的 JSON 文件中：
- `energy_data.json` - 精力分配数据
- `categories_config.json` - 分类配置
- `quadrant_tasks.json` - 任务数据

## 项目结构

```
energy_distribution/
├── main_pyqt5.py              # 应用入口
├── core/
│   ├── data_manager.py        # 数据管理
│   └── chart_generator.py     # 图表生成
├── gui_pyqt5/
│   ├── detail_view_qt.py      # 精力分配视图
│   ├── quadrant_view_qt.py    # 任务管理视图
│   ├── statistics_view_qt.py  # 统计视图
│   └── styles.py              # UI样式
├── data/
│   ├── energy_data.json
│   ├── categories_config.json
│   └── quadrant_tasks.json
└── requirements.txt           # 依赖配置
```

## 系统要求

- Python 3.8+
- PyQt5
- Matplotlib
- Pandas
- Numpy

## 许可证

MIT
